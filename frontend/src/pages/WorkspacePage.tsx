import { ShellPage } from '@/components/workspace'

export function WorkspacePage() {
  // @dvnull: Убран верхний переключатель API-разделов на /app; рабочий экран теперь открывается напрямую.
  return <ShellPage />
}
