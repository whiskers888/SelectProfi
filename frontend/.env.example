# API mode is fixed to server in runtime config
VITE_API_MODE=server

# Optional explicit backend URL (if empty, dev proxy "/" is used)
# VITE_API_BASE_URL=http://127.0.0.1:5268

# Optional mock latency (not used while server-only mode is enabled)
# VITE_MOCK_API_LATENCY=160
