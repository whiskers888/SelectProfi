# API mode: "server" for backend integration, "mock" for local in-memory API
VITE_API_MODE=mock

# Optional explicit backend URL (if empty, dev proxy "/" is used)
# VITE_API_BASE_URL=http://127.0.0.1:5268

# Mock response latency (ms), used only when VITE_API_MODE=mock
VITE_MOCK_API_LATENCY=160
